{
  "name": "Journi",
  "version": "1.0.0",
  "description": "A travel companion",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "dependencies": {
    "express": "^4.18.3",
    "morgan": "^1.10.0"
  }
}
